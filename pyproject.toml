[project]
name = "tetrix-take-home"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nathesh", email = "thejchess@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
tetrix-take-home = "tetrix_take_home:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]
